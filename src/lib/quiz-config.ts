/** Her oturumda sabit olarak seçilen yapay / gerçek soru sayıları (toplamları doğrulamak için ayrı sabitler). */
export const QUIZ_AI_PER_SESSION = 7;
export const QUIZ_REAL_PER_SESSION = 3;

export const DEFAULT_QUIZ_SIZE =
  QUIZ_AI_PER_SESSION + QUIZ_REAL_PER_SESSION;
